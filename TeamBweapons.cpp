#include "TeamBweapons.h"
#include "weapons.h"
int weaponsB:: TankH(int x)
    {
        int health;
        health=100-x;
        return health;
    }

int weaponsB:: RocketsH (int x)
  {
    int health;

    health=100-x;
    return health;
  }
int weaponsB:: ShipsH (int x)
  {
    int health;

    health=100-x;
    return health;
  }
  int weaponsB:: ArmorH (int x)
  {
    int health;

    health=100-x;
    return health;
  }

int weaponsB::MunitionsH (int x)
  {
    int health;
    health=100-x;
    return health;
  }
